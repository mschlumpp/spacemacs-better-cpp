(spacemacs|defvar-company-backends c++-mode)
(spacemacs|defvar-company-backends c-mode)
(spacemacs|defvar-company-backends cmake-mode)

(defvar better-cpp-default-mode-for-headers 'c++-mode
  "Default mode for open header files. Canbe `c-mode' or `c++-mode'.")
